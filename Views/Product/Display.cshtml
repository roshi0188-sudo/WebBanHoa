@model WebBanHoa.Models.Product

@{
    ViewData["Title"] = Model.Name;
}

<div class="container mt-4">
    <div class="mb-4 d-flex justify-content-between">
        <a asp-action="Index" class="btn btn-sm btn-light border"><i class="bi bi-arrow-left"></i> Quay lại cửa hàng</a>
        <div>
            <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary me-1" style="background-color: #BDB5D5; color: #000;">Sửa hoa này</a>
            <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-danger" style="background-color: #FFF3CD; color: #000;">Xóa</a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 text-center">
            <div class="border rounded-4 p-3 bg-white mb-3 shadow-sm">
                <img id="mainImg" src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/images/default_flower.jpg" : Model.ImageUrl)" class="img-fluid rounded-3" style="max-height: 440px; object-fit: contain;" />
            </div>

            <div class="col-md-6 text-center">
                <div class="border rounded-4 p-3 bg-white mb-3 shadow-sm">
                    <img id="mainImg" src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/images/default_flower.jpg" : Model.ImageUrl)" class="img-fluid rounded-3" style="max-height: 440px; object-fit: contain;" />
                </div>

                <div class="d-flex gap-2 justify-content-center">
                    <img src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/images/default_flower.jpg" : Model.ImageUrl)" class="img-thumbnail" style="width: 70px; height: 70px; object-fit: cover; cursor: pointer;" />
                </div>
            </div>
        </div>

        <div class="col-md-6 ps-md-4">
            <span class="badge bg-warning text-dark mb-2">BÁN CHẠY NHẤT</span>
            <h1 class="fw-bold text-dark mb-1">@Model.Name</h1>

            <div class="text-warning small mb-3">
                <i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i>
                <span class="text-muted small ms-2">(4.9/5 dựa trên 128 khách hàng)</span>
            </div>

            <div class="mb-4">
                <span class="text-danger fw-bold fs-2 me-3">@string.Format("{0:N0}đ", Model.Price)</span>
            </div>

            <div class="mb-2">
                <span class="badge bg-light text-secondary border rounded-pill px-3 py-2 fw-semibold">
                    <i class="bi bi-tag-fill text-primary me-1"></i> Danh mục: @(Model.Category?.Name ?? "Chưa phân loại")
                </span>
            </div>

            <p class="text-muted fst-italic mb-4 fs-5">"@Model.Description"</p>

            <div class="d-flex align-items-center mb-4">
                <div class="input-group" style="width: 140px;">
                    <button class="btn btn-outline-secondary" type="button" onclick="changeCount(-1)">-</button>
                    <input type="text" id="countInput" class="form-control text-center fw-bold" value="1" readonly>
                    <button class="btn btn-outline-secondary" type="button" onclick="changeCount(1)">+</button>
